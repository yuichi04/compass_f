export type ColorTypes = {
  primary: {
    light: string;
    natural: string;
    dark: string;
    contrast: string;
  };
  secondary: {
    light: string;
    natural: string;
    dark: string;
    contrast: string;
  };
  accent: {
    natural: string;
  };
  background: {
    default: string;
    dark: string;
  };
};
